# .gitlab-ci.yml

# 定义构建和部署阶段
stages:
  - build
  - deploy

# 编译/打包项目（例如，使用 npm 或 yarn）
build-job:
  stage: build
  artifacts:
    paths:
      - dist/  # 指定需要部署到 Pages 的目录
    expire_in: 1 hour  # 保留 artifacts 的时间，可选

# 部署到 GitLab Pages
pages:
  stage: deploy
  artifacts:
    paths:
      - dist/  # 指定 GitLab Pages 应该从哪个目录读取文件
  only:
    - master  # 只在 main 分支上部署 Pages（你可以根据需要更改）
